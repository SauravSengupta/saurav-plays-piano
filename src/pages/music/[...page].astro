---
import type { GetStaticPathsOptions, Page } from 'astro';
import { Image } from 'astro:assets'; // Import Astro's Image component for better handling
import { type CollectionEntry, getCollection } from 'astro:content';
import Pagination from '../../components/Pagination.astro';
import siteConfig from '../../data/site-config';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { sortItemsByDateDesc } from '../../utils/data-utils';

export async function getStaticPaths({ paginate }: GetStaticPathsOptions) {
    const projects = (await getCollection('music')).sort(sortItemsByDateDesc);
    return paginate(projects, { pageSize: siteConfig.projectsPerPage || 6 });
}

type Props = { page: Page<CollectionEntry<'music'>> };

const { page } = Astro.props;
const portfolio = page.data;
---

<BaseLayout title="Music" description="Original ambient piano compositions and releases by Saurav Plays Piano" showHeader={false}>
    
    <h1 class="mb-12 font-serif text-3xl font-light leading-tight sm:mb-16 sm:text-5xl sm:leading-tight text-main">
        Music
    </h1>
    
    <div class="grid gap-y-12 sm:gap-y-16 mt-8 mb-16">
        {portfolio.map((project) => (
            <a 
                href={`/music/${project.id}`} 
                class="group flex flex-col gap-4 sm:flex-row sm:items-start sm:gap-8"
            >
                {/* 1. IMAGE: Slightly reduced size (w-48 -> w-40) for a lighter feel */}
                <div class="shrink-0 relative overflow-hidden rounded-md shadow-sm sm:w-40 sm:h-40 aspect-square bg-muted/20">
                    {project.data.seo?.image?.src && (
                        <Image 
                            src={project.data.seo.image.src} 
                            alt={project.data.seo.image.alt || project.data.title}
                            width={400}
                            height={400}
                            class="h-full w-full object-cover transition-transform duration-700 ease-out group-hover:scale-105 opacity-90 group-hover:opacity-100"
                        />
                    )}
                </div>

                {/* 2. TEXT INFO: Now uses Serif hierarchy */}
                <div class="flex flex-col gap-2 pt-1">
                    
                    {/* TITLE: Switched to Serif + Light Weight (Matches Homepage Hero) */}
                    <h2 class="text-2xl font-serif font-light text-main decoration-1 underline-offset-4 group-hover:underline transition-all">
                        {project.data.title}
                    </h2>
                    
                    {/* DESCRIPTION: Lighter weight and softer opacity */}
                    <p class="text-main/80 text-base font-light leading-relaxed max-w-prose line-clamp-2">
                        {project.data.description}
                    </p>
                    
                    {/* LINK: Changed from "Uppercase Bold" to "Italic Serif" (More elegant) */}
                    <span class="mt-2 font-serif italic text-main/60 group-hover:text-main transition-colors flex items-center gap-2">
                        Listen <span class="not-italic text-xs">&rarr;</span>
                    </span>
                </div>
            </a>
        ))}
    </div>

    <Pagination page={page} class="my-16 sm:my-24" />
</BaseLayout>